# Clarify Log: Layered Testing Framework

Append-only record of smithy-clarify sessions for this RFC. Never modify prior sessions.

> **Note on `docs/testing-strategy.md` section numbers**: session entries below reference the strategy doc as it existed *at the time of the clarify pass*. The strategy doc was trimmed to strategic-only content in the same PR that landed this RFC — §8 (Gap analysis) and §9 (Roadmap) no longer exist there; that content migrated into this RFC. References to §8/§9 inside a session are historical, not pointers to current strategy-doc sections.

> **Note on milestone IDs**: session entries below reference milestones as `M-A` through `M-H`, the convention used during the initial drafting pass. The RFC was later renamed to the canonical Smithy `M<N>` convention (M1–M8) to match RFC 2026-001 and the audit checklist. Mapping: M-A→M1, M-B→M2, M-C→M3, M-D→M4, M-E→M5, M-F→M6, M-G→M7, M-H→M8. Session entries are kept as-written per the append-only policy.

### Session 2026-05-17

**Assumptions**:
- Personas: The Operator (sole human) in four modes — test author, CI failure triager, cassette refresher, scheduled-suite signal reviewer. Codex/Claude agents are secondary test authors via Smithy.
- Value vs. strategy doc: this RFC is the commitment + sequencing + per-milestone success criteria; the strategy doc remains the principles-first "why" reference.
- Contract docs live at `docs/subsystems/<name>/contract.md` (M-B creates the tree).
- Operating-philosophy rules apply with no exceptions: no interactive test runners, fail-loud, minimum required access for cassette-replay containers.
- [Critical Assumption] vitest for L0/L1 and Cucumber.js for L2/L3 is locked for this RFC; reselection out of scope.
- [Critical Assumption] $0 PR gate is a hard cost constraint; no live backend reachable from `npm test` or staged CI.
- Out of scope: eval rubrics, production SLOs, Smithy-side testing, TTY-only H-tests, framework reselection, Herald implementation itself, rewriting existing L0/L1 tests.
- M-A through M-G unblocked at RFC merge; M-H carries `Status: Blocked` on Herald RFC 2026-001 M4.
- Cassette on-disk root is `tests/cassettes/`.
- CI workflow: extend the existing `.github/workflows/ci.yml` into staged jobs (L0 fail-fast → L1 ∥ L2-cassette ∥ L3-cassette); a separate weekly workflow added in M-F.
- Risk register: M-C scope creep; cassette format wrong in hindsight; Herald-blocked M-H; contract-freshness CI noise; Cucumber.js parallelism unknown vs. vitest; Codex/Claude auth-shape divergence affecting redaction; throwaway-repo factory wall-clock unmeasured; `spawn-handoff.ts` is provisional code that may shift mid-RFC.
- Strategy-doc trim: §8, §9, and operational specifics of §6 migrate into this RFC; §2, §3, §4, §5 framework rationale, §6 cost principles, §7, and §10 stay in strategy. Bidirectional cross-links.

**Specification Debt deferred**:
- SD-001 — Initial M-F token budget cap (USD/run): recommend $5 soft / $20 hard. [Impact: High]
- SD-002 — Contract-freshness CI mechanism: Smithy-agent directive vs. TS AST diff. Recommend Smithy-agent first. [Impact: Medium]
- SD-003 — Cassette format v0 shape: raw JSONL HTTP capture vs. structured envelope. Recommend JSONL HTTP at the proxy layer; revisit if Codex/Claude auth split forces an envelope. [Impact: High]
- SD-004 — Quarantine SLA escalation path for solo operator beyond visibility in weekly report. [Impact: Medium]
- SD-005 — Whether M-C concurrent-dispatch variant counts as one scenario or two for the scope cap. [Impact: High]
- SD-006 — M-F structured cost-summary publish target: Actions job summary, committed JSONL file, or Herald event. [Impact: Low]
- SD-007 — Whether M-D L1 Hatchery tests have any hard dependency on M-B Hatchery `contract.md` being drafted first. [Impact: Medium]
